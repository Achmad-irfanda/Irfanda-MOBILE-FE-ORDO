// apps recent lead model

class RecentLeadModel {
  String imageLeading, name, status, datelead, imageTrailing;
  int nominal;

  RecentLeadModel(
      this.imageLeading, this.name, this.status, this.datelead, this.nominal, this.imageTrailing);
}
